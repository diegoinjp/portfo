---
type Props = {
  styles?: string;
};
const { styles } = Astro.props as Props;
---

<h2
  class={`dark:first-letter:text-accent-400 first-letter:text-accent-500 text-3xl font-medium tracking-tight first-letter:text-6xl sm:text-4xl ${styles}`}
>
  <slot />
</h2>
